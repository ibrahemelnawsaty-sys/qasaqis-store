<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

// نموذج «التثبيت» الجديد لصفحة /books: books.sort_order = 0 يعني «غير مثبَّت» (يُرتَّب
// بالأحدث)، و> 0 يعني «مثبَّت في تلك الرتبة». الميزات السابقة كانت ترقّم كل الكتب 1..N
// (ترتيب معرّفات لا معنى له للعميل)، فلو بقيت تلك القيم لعُدّت كلّها «مثبَّتة» وانقلب ترتيب
// /books والكاروسيلات إلى الترتيب القديم بدل «الأحدث». نصفّر العمود لتبدأ مجموعة التثبيت
// فارغةً فيصحّ «الأحدث افتراضيًّا»، ثم يثبّت المالك ما يريد يدويًّا.
//
// آمنة: تُعيد ضبط عمود واحد فقط، لا تحذف أي صفّ (الدستور 3.3). ترتيب كتب الأقسام منفصل
// تمامًا في جدول category_book_positions ولا يتأثّر.
return new class extends Migration
{
    public function up(): void
    {
        DB::table('books')->update(['sort_order' => 0]);
    }

    public function down(): void
    {
        // لا عكس: القيم القديمة كانت ترقيمًا آليًّا (1..N) بلا معنى، ولا فائدة من استعادتها.
    }
};
