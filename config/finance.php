<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | نسبة خصم الشراء الافتراضية (٪)
    |--------------------------------------------------------------------------
    |
    | تُستعمل لتقدير تكلفة كتابٍ بلا سعر شراء مُدخَل حين لا نسبة خاصّة لدار نشره:
    |   التكلفة الافتراضية = السعر × (١ − النسبة / ١٠٠).
    | النسبة الخاصّة بكل دار (publishers.cost_discount_percent) تتقدّم على هذه.
    | ٢٥ تعني «الشراء بخصم ٢٥٪ عن سعر البيع» (هامش ربح ≈ ٢٥٪).
    |
    */
    // تُمرَّر كما هي (BookCostResolver يحوّلها إلى نص قبل حساب bcmath) — بلا (float)
    // كي لا يدخل أي عدد عائم مسار التكلفة (اتساق «decimal في كل مكان»).
    'default_cost_discount_percent' => env('FINANCE_DEFAULT_COST_DISCOUNT', 25),
];
