<?php

declare(strict_types=1);

/*
| إعدادات تشغيلية للطلبات. لا أسرار هنا (بند 4.3) — قيم تشغيلية فقط.
*/

return [

    // مهلة الرابط الموقّت المُعاد توليده من صفحة «تتبّع الطلب» (بالدقائق).
    // قصير العمر لتقليل خطر تسريب رابط يكشف تفاصيل الطلب (M3).
    'track_link_ttl_minutes' => (int) env('ORDER_TRACK_LINK_TTL', 60),

];
