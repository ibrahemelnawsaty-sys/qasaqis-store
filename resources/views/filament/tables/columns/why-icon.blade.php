{{-- عمود الأيقونة في جدول «ليه الأمهات بيحبونا». نستعمل نفس مكوّن الواجهة
     فلا تتفرّق النسختان: ما يراه الأدمن هنا هو ما سيراه الزائر هناك.

     الأيقونة نفسها aria-hidden (زخرفية)، فلولا الاسم بجانبها لكانت الخلية
     فراغًا تامًّا لقارئ الشاشة — ولا سبيل لمعرفة أيقونة الصفّ. الاسم يفيد
     البصر أيضًا: يربط الشكل بالمفتاح الذي يختاره الأدمن من القائمة.

     بلا لون صريح: الأيقونة currentColor فترث لون نصّ الجدول، فتعمل في وضعَي
     Filament الفاتح والداكن معًا. (‎--primary-600‎ في Filament قنوات RGB لا لون،
     فوضعها في color يُنتج قيمة غير صالحة.) --}}
@php
    $key = $getState();
    $label = \App\Filament\Resources\WhyItemResource::iconOptions()[$key] ?? null;
@endphp

<div class="flex items-center gap-2">
    <x-why-icon :name="$key" :size="26" />
    <span class="text-sm text-gray-600 dark:text-gray-400">
        {{-- قيمة قديمة (إيموجي) لا اسم لها في المكتبة: نعرض القيمة نفسها --}}
        {{ $label ?? $key }}
    </span>
</div>
